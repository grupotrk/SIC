'use client'
import { useState } from 'react'
import { useRouter } from 'next/navigation'
export default function AdminChangePassword(){
 const router=useRouter(); const [p,setP]=useState('');const [c,setC]=useState('');const [error,setError]=useState('');const [loading,setLoading]=useState(false)
 const submit=async(e:React.FormEvent)=>{e.preventDefault();setError('');if(p.length<12){setError('Usá al menos 12 caracteres.');return}if(p!==c){setError('Las contraseñas no coinciden.');return}setLoading(true);const r=await fetch('/api/admin-change-password',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({password:p})});setLoading(false);if(!r.ok){setError('No se pudo cambiar la contraseña.');return}router.replace('/admin/dashboard')}
 return <main className="min-h-screen bg-[#07111F] text-white flex items-center justify-center px-6"><div className="w-full max-w-md rounded-xl border border-slate-600 bg-[#1B263B] p-8"><div className="text-2xl font-black text-[#60A5FA] mb-1">SIDEA ADMIN</div><h1 className="text-xl font-bold mb-2">Creá tu contraseña personal</h1><p className="text-sm text-slate-300 mb-6">La clave provisoria ya no se podrá usar después de este cambio.</p>{error&&<div className="mb-4 rounded border border-red-500/60 bg-red-950/40 p-3 text-sm">{error}</div>}<form onSubmit={submit} className="space-y-4"><input type="password" required minLength={12} value={p} onChange={e=>setP(e.target.value)} placeholder="Nueva contraseña (12+ caracteres)" className="w-full rounded-lg border border-slate-600 bg-[#0D1B2A] px-4 py-3"/><input type="password" required minLength={12} value={c} onChange={e=>setC(e.target.value)} placeholder="Repetir contraseña" className="w-full rounded-lg border border-slate-600 bg-[#0D1B2A] px-4 py-3"/><button disabled={loading} className="w-full rounded-lg bg-[#2563EB] px-4 py-3 font-bold disabled:opacity-60">{loading?'Guardando...':'Guardar y entrar'}</button></form></div></main>
}
